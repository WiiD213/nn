digraph G {
    // Настройка внешнего вида
    graph [fontname = "Arial", rankdir = LR, splines = true, nodesep = 0.8];
    node [fontname = "Arial", shape = record, style = filled, fillcolor = lightblue];
    edge [fontname = "Arial"];

    // Таблица Автопарк
    Автопарк [label = "Автопарк|{<id>ID автомобиля (PK)|Марка|Модель|Год выпуска|Гос. номер|Статус}"];

    // Таблица Данные_по_пробегу
    Данные_по_пробегу [label = "Данные по пробегу|{<id>ID записи (PK)|<car_id>ID автомобиля (FK)|Дата|Пробег|Тип обслуживания}"];

    // Таблица Отчет_по_автопарку
    Отчет_по_автопарку [label = "Отчет по автопарку|{<id>ID отчета (PK)|<car_id>ID автомобиля (FK)|Дата отчета|Статус ТО|Расход топлива}"];

    // Новая таблица Аренда
    Аренда [label = "Аренда|{<id>ID аренды (PK)|<car_id>ID автомобиля (FK)|Время начала TEXT|Время окончания TEXT}"];

    // Связи
    Автопарк:id -> Данные_по_пробегу:car_id [label = "1:N"];
    Автопарк:id -> Отчет_по_автопарку:car_id [label = "1:N"];
    Автопарк:id -> Аренда:car_id [label = "1:N"];
} 